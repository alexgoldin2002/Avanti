export default function LogoExportLayout({ children }: { children: React.ReactNode }) {
  return (
    <div style={{ display: 'inline-block', background: 'transparent' }}>
      {children}
    </div>
  )
}
